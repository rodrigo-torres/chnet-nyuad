This program transforms 42374 entries as would be transformed in the real DAQ protocol for the XRF.
Running,
	time ./peformance

suggests the program takes ~65 ms to process the task.

With half the entries, the same command seems to take ~ 32 ms. Suggesting linear complexity.

